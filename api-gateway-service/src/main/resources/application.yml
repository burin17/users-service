server:
  port: 8080

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

spring:
  cloud:
    gateway:
      routes:
        - id: users-crud-service
          uri: lb://users-crud-service
          predicates:
            - Path=/users/**
  application:
    name: api-gateway
